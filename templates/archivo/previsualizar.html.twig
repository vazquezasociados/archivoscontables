{% extends 'base.html.twig' %}

{% block title %}Previsualización -{{ archivo.titulo }} {% endblock %}

{% block body %}
    <div class="alert alert-danger mt-5 text-center">
    
        <img src="{{ asset('build/images/Vazquez_logo_a.svg') }}" alt="Vazquez Contadores" class="login-logo">

        <h2>{{ archivo.titulo }}</h2>

        {# <iframe src="{{ path('app_archivo_previsualizar_pdf', {id: archivo.id}) }}"
                width="100%" height="600px" style="border: none;"></iframe> #}

        <div class="mt-3">
            <a href="{{ path('app_archivo_descargar_directo', {id: archivo.id}) }}" 
            class="btn btn-primary">
                📥 Descargar
            </a>
        </div>
    </div>
{% endblock %}
