<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Memo</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            font-size: 12px;
            margin: 40px;
            color: #222;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding-bottom: 10px;
            border-bottom: 1px solid #ccc;
        }
        .logo {
            width: 20px; 
            height:auto;
        }
        .info {
            text-align: right;
            font-size: 12px;
        }
        .info p {
            margin: 2px 0;
        }
        h1 {
            text-align: center;
            margin: 25px 0 15px 0;
            font-size: 16px;
            font-weight: 700;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        table th, table td {
            border-bottom: 1px solid #F0F0F0;
            padding: 6px 8px;
            text-align: left;
        }
        table th {
            background: #f7f7f7;
            font-weight: 500;
        }

        .firma {
            margin-top: 10px;
            display: flex;
            justify-content: space-between;
            font-size: 12px;
        }
        .firma .linea {
            border-bottom: 1px dashed #CCC;
            margin-bottom: 4px;
            height: 0;
        }

        .page-break {
            page-break-after: always;
        }
    </style>
</head>
<body>
    <header>
        <div>
            <img src="{{ logoSrc }}" alt="Logo" class="logo">
        </div>
        <div class="info">
            <p><strong>Fecha:</strong> {{ memo.createdAt|date('d/m/Y') }} </p>
            <p><strong>Estado:</strong> {{ memo.estado }} </p>
            <p><strong>Cliente:</strong> {{ nombre }}</p>
        </div>
    </header>

    <h1>MEMO</h1>

    <table>
        <thead>
            <tr>
                <th>Descripción</th>
                <th>Detalle</th>
                <th>Período</th>
            </tr>
        </thead>
        <tbody>
           {% for item in memo.lineItems %}
            <tr>
                <td>{{ item.item.descripcion }}</td>
                <td>{{ item.descripcionAdicional }}</td>
                <td>{{ item.periodo ? item.periodo|date('m/Y') : '' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <div class="firma">
        <table style="width: 100%; border: none; margin-top: 40px;">
            <tr>
                <!-- Columna izquierda vacía -->
                <td style="width: 60%;"></td>
                
                <!-- Columna derecha para firmas -->
                <td style="width: 40%;">
                    <p style="margin-bottom: 5px;">Firma</p>
                    <div class="linea"></div>
                    
                    <p style="margin-top: 20px; margin-bottom: 5px;">Aclaración</p>
                    <div class="linea"></div>
                </td>
            </tr>
        </table>
    </div>
    
    </br><br>
    </br><br>
    <!-- réplica -->

        <header>
        <div>
            <img src="{{ logoSrc }}" alt="Logo" class="logo">
        </div>
        <div class="info">
            <p><strong>Fecha:</strong> {{ memo.createdAt|date('d/m/Y') }} </p>
            <p><strong>Estado:</strong> {{ memo.estado }} </p>
            <p><strong>Cliente:</strong> {{ nombre }}</p>
        </div>
    </header>

    <h1>MEMO</h1>

    <table>
        <thead>
            <tr>
                <th>Descripción</th>
                <th>Detalle</th>
                <th>Período</th>
            </tr>
        </thead>
        <tbody>
           {% for item in memo.lineItems %}
            <tr>
                <td>{{ item.item.descripcion }}</td>
                <td>{{ item.descripcionAdicional }}</td>
                <td>{{ item.periodo ? item.periodo|date('m/Y') : '' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="firma">
        <table style="width: 100%; border: none; margin-top: 40px;">
            <tr>
                <!-- Columna izquierda vacía -->
                <td style="width: 60%;"></td>
                
                <!-- Columna derecha para firmas -->
                <td style="width: 40%;">
                    <p style="margin-bottom: 5px;">Firma</p>
                    <div class="linea"></div>
                    
                    <p style="margin-top: 20px; margin-bottom: 5px;">Aclaración</p>
                    <div class="linea"></div>
                </td>
            </tr>
        </table>
    </div>

</body>
</html>
